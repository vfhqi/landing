# Decisions — SA - IA A&J Memo Batching Automation

**Convention:** append-only. Locked by Richard's plan sign-off, 2026-06-12.

| ID | Decision |
|---|---|
| D-AJ-1 | Triaging: `fast_track_ij` always true; **Change Forces only** is the required floor (fix 01-SYSTEM-OVERVIEW which still says CF+BF); zero self-initiated queries; the agent flags would-be gaps in the self-review debrief (§4/§5). |
| D-AJ-2 | ESA/DD: 10-stage workflow (E1-E10) with two synchronous Richard Q&A gates (E1 sponsor thesis; E4 approve key questions). |
| D-AJ-3 | Build Triaging first; layer ESA after Triaging proven supervised. |
| D-AJ-4 | Manual brief-card trigger only (Richard-initiated); no auto-trigger on research completion (a morning-briefing "memo-ready" nudge is allowed). |
| D-AJ-5 | Supervised first build; unattended as the target (Triaging only — ESA/DD are supervised by design due to the gates). |
| D-AJ-6 | Named workflow stages (Triaging T1-T6; ESA/DD E1-E10) tracked in `metadata.json` and shown in the repository viewer. |
| D-AJ-7 | KQ queries (E5) briefed to the conveyor at priority "high"; needs a `priority` field in card + hopper. |
| D-AJ-8 | Floor-QC fix loop capped at 3 iterations; escalate to the morning briefing on cap breach. |
| D-AJ-9 | Memo pipeline extends the existing conveyor as inbox rungs (5-triaging-memo, 6-esa-memo, 7-memo-judge, 8-memo-publish, 9-memo-closed); one card = one job; atomic FUSE-safe moves. |
| D-AJ-10 | Mechanism = Cowork agent per stock (multi-turn). Batches API parked as a future Triaging-only cost optimisation. |
| D-AJ-11 | Cohort Context Pack = the EXISTING D-BATCH-25 cache, promoted to default-on and enriched to a grade-anchoring pack that includes peers' prior MEMO GRADES + comparison tables. NOT a parallel mechanism. |
| D-AJ-12 | Analytical-quality Judge agent is a standing rung from day one. Sonnet, non-deep, **medium effort**, **FOUR checks** (grounding incl. price-used; specifics-retention; grade calibration cohort-anchored + roll-up; summary-body consistency). PASS/REVISE/ESCALATE; escalate to morning briefing; effort targeted by the writer's conviction tiers. |
| D-AJ-13 | Bind the agent's analytical design to the stable Thinking SOP now; the Stage-1 ship-test waits until the Bravida ESA validates the live presentation layer (SOP-03 V3.2 / SOP-04 V4.3 / new renderer + parser). |
| D-AJ-14 | **Render-and-Visual-QC is its own station** (not folded into the Judge) — headless render via watson-qc-render.sh + parser-clean check + `browser_qc_confirmed` flag; required because the QC script hard-fails a memo with no confirmed visual sign-off. *(Mechanism updated by D-AJ-18, 2026-06-15: enforcement is real and the `browser_qc_confirmed` flag is retired — the checker self-triggers the render.)* |
| D-AJ-15 | **Gate release is continuous and event-driven, not an overnight wait** (Richard's 12-Jun correction). E6/T2: a waiting registry + the research conveyor's publish step pushes a card-advance when a memo's blocking report set completes; a cheap deterministic poller (no LLM) backstops; timeout→park. The expensive writer agent fires once, on gate-open. Low-token by construction. |
| D-AJ-16 | **Partial key-question completion (E6) = Option B (write-with-flag), NOT hold-and-park** (Richard, 2026-06-15). If an approved key-question report fails after the conveyor's retry/backoff is exhausted (genuine lack of info OR persistent technical failure), the memo PROCEEDS to writing rather than parking; the unanswered question is carried as an explicit gap in the self-review debrief §4 (constrained/impeded analysis) + §5 (recommended further work) and CLEARLY FLAGGED as a research error, never a silent omission. Supersedes the §J "park with a flag" default in the ESA/DD walkthrough. Retry/backoff still runs FIRST, so proceed-and-flag fires only after genuine exhaustion. Richard's words: "if one fails due to lack of info, clear rest of A&J needs to just continue." |

| D-AJ-17 | **Memo Writer model config: Sonnet, HIGH effort, extended thinking ON** (Richard, 2026-06-15). Quality over cost; the Writer is the analytical core. Reserve "max" effort for writer-flagged high-uncertainty cases + Deep-Dive memos (targeted, not blanket). Judge stays non-deep / medium per D-AJ-12. Runtime model split: **build on Opus, run on Sonnet** (Richard's "tilt higher for build/complex work" steer). |

| D-AJ-18 | **Render + Visual QC is now a REAL script-enforced HARD gate** (2026-06-15 reconciliation of the SOP-vs-script divergence; Richard confirmed "yes, do that"). `generate_qc_audit.py` triggers `render_qc_gate.py` by default: the gate builds the memo's real repository viewer (`gen_memo_viewer.generate`) into a throwaway temp dir and renders it headless (`watson-qc-render.sh`), HARD-failing a viewer that does not build, collapses to a flat/ratingless structure, or paints with console/page errors or blank. **UNAVAILABLE (renderer cold) also BLOCKS** — an unattended run must never ship an *unrendered* memo; the cold Chromium self-heals and the pipeline's retry/backoff covers the re-run. Supersedes amendment B's "render-QC is NOT a script gate" finding and **retires the `browser_qc_confirmed` sidecar field** (the checker self-triggers the render; no manual Chrome flag). The Writer's intermediate floor-QC fix iterations may pass `--render-qc off`; the final pre-publish audit and every unattended run leave it ON. **Companion fix:** `check_ratings_json` was repaired to accept BOTH canonical `ratings.json` schemas (the flat renderer schema AND the geo-key-nested writer schema); it had regressed to flat-only and was hard-failing every correctly-authored memo (caught on the KPN re-audit, 2026-06-15). |

## Open (not yet decided)
- *(none — D-AJ-16/17 closed the prior open items; the render-QC SOP-vs-script divergence is closed by D-AJ-18, 2026-06-15)*

| D-AJ-19 | **Stage-0 plumbing built + the Writer model config ENFORCED IN CODE** (2026-06-16). orchestrate_memo.py bakes D-AJ-17 (Sonnet, HIGH effort, extended thinking ON) into the writer spawn-config JSON + the materialized task-prompt header, so a cold-restart agent cannot silently drop to standard. Scripts: memo_state.py, generate_cohort_pack.py, assemble_memo_context.py, gate_monitor.py, orchestrate_memo.py. |
| D-AJ-20 | **Doc-07 Station-3 marker reconciled** (2026-06-16): the retired `browser_qc_confirmed` is replaced by the STATE-block `render_qc` field (PASS|FAIL|UNAVAILABLE|null), written from generate_qc_audit.py's exit code. Doc 07 §B/§C/CR-09 references are superseded (reconciliation block appended to doc 07). |

| D-AJ-21 | **Memo structural-format reform AGREED** (Richard, 2026-06-16 PM) — applies to ALL memos via the SOPs/skeletons/QC, not OBEL-specific. (1) NO prose paragraphs anywhere (everything bullets + nested sub-bullets; carve-outs for headings/tables/hero-fields/source-lines/list lead-ins; "prose" = >1 sentence or >40w non-bullet body block). (2) CQ ratings on judgement-bearing Core Questions across all 3 stages (HARD; factual CQs tagged). (3) Exec-summary italics, B.3 Master Ratings, and all section summaries -> bullets (summaries keep the indented colour block + italic/demi-bold, bullets inside). (4) Element-grade/counter-view/downstream + "Group N light" + "deeper analysis" blocks -> bulleted + signposted. (5) Acronyms/shorthand written with a plain-English description EVERY time, acronym retained (SOFT); ban source shorthand like "L4"; spell out Element names. (6) Bullet length ~30 target / 40 HARD ceiling. Enforcement: HARD = no-prose + 40-word ceiling + missing CQ ratings; SOFT = acronym-without-description. HARD gate forces the writer fix-loop (not a defects dump); supervised mode surfaces sticking points to Richard and iterates to standard. Implement in the next Opus window, then regenerate the 3 memos. Spec: FEEDBACK-DECISIONS-2026-06-16-PM-memo-format-reform.md. |

| D-AJ-22 | **Format reform IMPLEMENTED + tested on OBEL-BE** (2026-06-16, Opus). D-AJ-21 shipped across: the 3 SOPs (02/03/04 — no-prose block, 40-word HARD ceiling, CQ-grade rule, acronym-every-time); the 3 skeletons (TRIAGING hand-rebuilt, ESA + Deep-Dive transformed — every heading preserved, summaries -> `> -` bulleted colour blocks); generate_qc_audit.py (4 new checks: no-prose HARD, 40-word ceiling HARD, CQ-ratings HARD, acronym SOFT — unit-tested: 0 false positives on reformed content, true positives on the old prose memo); orchestrate_memo.py writer prompt (FORMAT block). **VIEWER CHANGE (not in the original blast radius):** memo_spine_parser.py + memo_viewer_render.py + memo-viewer-base.html extended so a blockquote that contains bullets renders as a bulleted judgement-summary box (backward-compatible: prose summaries unchanged; spine IDENTICAL on the old OBEL memo). OBEL-BE rebuilt under the new format (Sonnet HIGH writer, recast from the judged-sound prior memo + 6 judge fixes folded in): text-QC 0 HARD / 12 SOFT; render-QC PASS (17 elements, 19 signals, no errors); UI-QC clean (no horizontal overflow, search/toggles/nav-scroll intact); Judge PASS (no recast defects, 6 prior defects resolved). HELD for Richard's format sign-off before fan-out to SCMN-CH + PROX-BE. Repo deploy of the viewer change HELD until sign-off. |

| D-AJ-23 | **Format reform v2 feedback received (Richard, 2026-06-16 PM) — 16 structural items, NOT yet built.** Full fidelity capture: CONVERSATION-CAPTURE-2026-06-16-PM-FORMAT-REFORM-AND-V2-FEEDBACK.md. Headlines: underline/highlight/italic minimums become HARD gates (highlight check was disabled by render_target flag; italic unchecked; acronym check too weak — all owned); consolidate multiple judgement-summary boxes into ONE per area; CQ main bullet + RA each carry a one-line ANSWER summary (RA duplicated in summary box AND at the RA); visual hierarchy via font-weight + subtle colour (Section/Group>Element>RA>CQ); sub-bullets numbered not lettered; ALL inline [A-F] render as colour pills incl. BLUF; fix E7 RA1 CQ1 nesting+capitalisation; line-break before new sub-sections; signpost EVERY bullet; E8 spell-out shorthand (HARD); TOC shows ALL framework sections incl n.a. as hyperlinks; fix TOC buttons (Element details / All RAs on / All CQs on / Audit). MUST propagate to ESA + DD. 8 open design Qs listed in the capture Part 5 — resolve before the coordinated build. Viewer deploy + SCMN/PROX fan-out STILL held. |

| D-AJ-24 | **Format reform v2 BUILT + OBEL-BE regenerated to full v2** (2026-06-16 PM, Opus — applied in code but not written here until the 2026-06-17 reconciliation). The 16-item feedback (D-AJ-23) shipped as six new emphasis/structure HARD gates in generate_qc_audit.py (underline floor 15%, highlight floor 30%, italic floor 20%, signpost-every-bullet, bullet-capitalisation, element-name spell-out) plus viewer changes (consolidated judgement box per element, RA answer-lines, inline grade pills, numbered nested sub-bullets, RA-vs-CQ weight differentiation, all-elements TOC). OBEL-BE rebuilt: text-QC 0 HARD, render-QC PASS (25 elements, 37 rating signals). KNOWN PARTIAL F5: the one-line ANSWER sits on each RA main bullet but only in a sub-bullet on each Core Question (~49 CQs), pending Richard. Emphasis density underline 16.8% / highlight 56% / italic — highlight likely too heavy, pending Richard. Viewer + gates STILL NOT deployed (held for Richard's format sign-off). Full record: CONVERSATION-CAPTURE-2026-06-16-PM-FORMAT-REFORM-AND-V2-FEEDBACK.md Parts 6-8. |

| D-AJ-25 | **SDIP.B-SE (Sdiptech) Triaging memo produced + deployed live** (2026-06-16 evening). Recommendation PARK; headline ACH FALSE FRIEND [D]; element grades E1=B/E5=C/E7=D/E8=C/E11=C. QC clean (0 HARD, 8 SOFT, 50 PASS); headless render PASS; deployed via targeted fast-deploy (commit 431c1f8, both URLs 200 OK). GAP: no real-browser visual QC (headless only; low risk). FORMAT NOTE: renders 17 elements / 48 rating signals, i.e. it appears built under the EARLIER (pre-v2) format, NOT the v2 all-element TOC used on OBEL-BE — so the live repository currently carries one v1-format memo (SDIP) and one undeployed v2-format memo (OBEL). Normalise on the v2 viewer deploy: re-render/verify SDIP under the v2 viewer for cross-memo consistency. |

| D-AJ-26 | **Mechanism confirmation + track separation (reaffirmed 2026-06-17).** "Live" rides on the Cowork-agent-per-stock multi-turn pipeline (this project), per D-AJ-10. The overnight Batches-API track (project "IA — Analysis and Judgement Memos", blocked on its Q1-Q4) stays PARKED as a future Triaging-only cost optimisation and is NOT on the path to live. The 2026-06-17 04:05 handoff that framed next steps around the Batches-API build was a compaction drift onto the parked branch; it is superseded by this project's plan. |

| D-AJ-27 | **F5 (per-question one-line answer) — IMPLEMENT, in the Sonnet build session** (Richard, 2026-06-17). Add a one-line ANSWER on each Core Question MAIN bullet (after the grade pill, before the numbered evidence sub-bullets), sourced from each Core Question's existing "Net ... verdict" sub-bullet (~49 on OBEL-BE). Enforce as BOTH a SOP/skeleton rule (CQ main-bullet pattern = signpost + question + grade pill + one-line answer) AND a QC-gate check (every in-scope judgement Core Question main bullet carries an answer clause); propagate to ESA + Deep-Dive. DESIGN specified here/in doc 10; EXECUTION (gate code + OBEL regen) is Sonnet. Rationale for Sonnet not Opus: content/format execution, pairs with the v2 deploy + the emphasis trim on OBEL (D-AJ-28). |

| D-AJ-28 | **Emphasis density CAPPED (Richard, 2026-06-17): highlight max 40%, italics max 20%.** Converts the emphasis gates from floor-only to a BAND (the gate now HARD-fails for TOO MUCH emphasis as well as too little). Highlight band = 30%-40% of body bullets (floor unchanged; new HARD cap 40%). Italics: Richard set max 20%, but the existing floor was ALSO 20% — that collapses to an impossible exactly-20% knife-edge; CORRECTION (Watson, proceeding unless overridden) = italics band 10%-20% (floor lowered to 10%, Richard's 20% as the HARD cap). Underline left floor-only at 15% (no cap requested; not flagged as heavy). Unit = % of body bullets carrying that emphasis. Bands kept wide enough to avoid writer fix-loop thrash. OBEL-BE (currently 56% highlight / 100% italics) EXCEEDS the new caps and needs an emphasis-reduction pass, done with the F5 pass in Sonnet. |

| D-AJ-29 | **Emphasis bands FINALISED (Richard, 2026-06-17; supersedes the band numbers in D-AJ-28).** Underline band = 15%-25% of bullet WORDS (floor 15% unchanged; NEW HARD cap 25%). Italics band = 15%-25% of body BULLETS (supersedes the earlier 10%-20%; floor 15%, HARD cap 25%). Highlight band UNCHANGED at 30%-40% of body bullets. All three are HARD bands (fail too-little AND too-much); 10 points wide to avoid writer fix-loop thrash. Unit note: underline = % of words; highlight + italics = % of bullets. OBEL-BE compliance: underline 17% OK; italics 100% (over) + highlight 56% (over) -> trim in the Sonnet F5 pass. |

| D-AJ-30 | **Q1=B: ESA writer upgrades pre-v2 Triaging prior inline** (Richard, 2026-06-17 PM). BRAV-SE Triaging memo (2026-06-13) is pre-v2 format. Decision: do NOT recast before the ESA run. Instead, let the ESA writer re-apply v2 emphasis to all carried sections as it deepens them (doc 10 L2 option b). ESA_WRITER_PROMPT_TMPL already carries the explicit instruction "If prior memo is pre-v2 format, re-apply v2 emphasis to all carried sections as you deepen them (doc 10 L2)." No additional build required. |

| D-AJ-31 | **Q2=A: Real research conveyor for E6 in Step D** (Richard, 2026-06-17 PM). For the live ESA test, E5 submits KQ research brief cards to the existing four-agent research conveyor (AlphaSense Deep Research pipeline). Step D spans sessions: E1-E4 today; E6 wait while AS processes; writer + QC + publish after E6 opens. Rationale: "test things properly from here, faithful to the SOP." gate_monitor scheduled watchdog set for E6 polling. |

| D-AJ-32 | **Q3: Archive two IA-initiated ESA research files** (Richard, 2026-06-17 PM). The two pre-existing files at Files/BRAV-SE/A-J-memo/ (BRAV-SE-ESA-IA-initiated-organic-decomposition-2026-06-05.md and BRAV-SE-ESA-IA-initiated-receivables-scenario-analysis-2026-06-05.md) are moved to Files/BRAV-SE/A-J-memo/archived-2026-06-17/ before the ESA pipeline run. The E3 key-question identification agent does NOT read them; it works from the four report families only. |

### D-ESA-1 — E3→E6 gate missing from advance() [2026-06-17 PM]
**Decision**: Fix immediately, add mock tests, roll back state, re-run live test.
**Rationale**: advance() went directly from context_assembled to writer dispatch for ESA/DD, bypassing the KQ research loop entirely. Writer would have fired without KQ research answers embedded. Caught during live Step D test.
**Fix**: orchestrate_memo.py lines 294-305 — gate added before writer dispatch; mock mode and Triaging bypass; CR-ESA-8a/b/c/d cover all three blocked states plus the pass-through.

### D-AJ-33 — BRAV-SE E4 KQ refinements [2026-06-17 PM]
**kq-2**: Narrowed from abstract tailwind size to incremental revenue (now vs next 3y vs past).
**kq-4**: Removed valuation question; narrowed to SS consensus vs guidance delta only (how much is SS pricing above what management has communicated).

## Step D Live-Fire Decision — 2026-06-18

**Decision:** Accept 2 permanent HARD FAILs (italic + signpost) as QC code bugs, not memo defects.

**Evidence:** 
- `_ITALIC_RX` in generate_qc_audit.py matches `**bold:**` bullets as italic → inflates italic% to 49%
- Signpost requires `**label:**` on every bullet — but adding bold to all bullets makes italic count explode
- These two checks are mutually exclusive at scale; contradiction is in the QC code, not the memo content
- Both issues confirmed in prior triaging memo run; accepted as permanent by Watson per SOP

**Outcome:** BRAV-SE ESA memo deployed with 70 PASS / 2 irreducible HARD FAIL. Step D pipeline test COMPLETE.
