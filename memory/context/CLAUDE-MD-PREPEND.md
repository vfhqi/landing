# CLAUDE.md PREPEND — add this to the very top of `.claude\CLAUDE.md`

The block below should appear **above** the "# Memory" heading in the user's private CLAUDE.md so every session hits it first. App-internal CLAUDE.md is outside the session mount, so this canonical copy lives in COWORK. Paste on next CLAUDE.md edit.

---

```markdown
# CONTEXT WINDOWS + SOP — MISSION CRITICAL

**Applies to every role, every session. Do not skip.**

- **AIM:** Never lose knowledge in a temporary context window. Projects persist over multiple days — losing continuity is project-level failure.
- **REQUEST 1:** Do NOT write to context-window / session memory unless explicitly asked. Always check which layer to write to.
- **REQUEST 2:** Default to durable memory files under `C:\Users\richb\Documents\COWORK\`. Can be restarted weekly if stale, but nothing critical lives only in session memory.
- **REQUEST 3:** Save continually — maximum **5-minute** gap during active work. Decisions, corrections, scope, structure, judgements → persist immediately.
- **REQUEST 4:** Every role (RESEARCHER, EA, FA, APM, HPC, SA) — no exceptions.

**Full rule + incident log + save triggers:** `memory/context/context-windows-sop.md`

**When unsure where to save: COWORK memory/.**

---
```
