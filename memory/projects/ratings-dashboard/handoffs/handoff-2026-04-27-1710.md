# Session Handoff — 27 Apr 2026 17:10
## Ratings Dashboard | SA + APM | EXECUTION

## Optimal Restart Prompt — copy-paste into next session

```text
Good morning. Watson, you are resuming RATINGS DASHBOARD from 27-Apr-26 17:10 UK.

Role: SYSTEMS ARCHITECT (primary). APM as secondary/background.
Mode: EXECUTION.
Agenda mode rationale: Executing a defined set of RESEARCH STAGES tab improvements — no new architecture, all changes are enhancements to an existing working feature.

=== ORIENTATION (read in this order before responding) ===
1. COWORK/.claude/CLAUDE.md (always — operating context)
2. COWORK/memory/session-handoffs/latest.md (this handoff pointer)
3. COWORK/memory/projects/ratings-dashboard/state.md (live project state)
4. COWORK/memory/projects/ratings-dashboard/decisions.md (locked decisions — never re-ask)
5. COWORK/memory/projects/ratings-dashboard/handoffs/handoff-2026-04-27-1710.md (this full handoff)
6. COWORK/databases/scripts/patch-coverage-tab.py (the patcher — this is the file to modify)
7. COWORK/databases/coverage-data.json (the data source — may need scanner rebuild)
8. COWORK/databases/scripts/build-coverage-data.py (the scanner that builds coverage-data.json)
9. .auto-memory/MEMORY.md (always — pointers to feedback rules)

=== WHERE WE LEFT OFF (verbatim) ===
Dashboard is at 5,201,006 bytes locally. 9 stocks in masterData (NVTK, HTRO, IGG, ENAV, EKTA, DCC, GET, PRY, DIE). 8 tickers in memoData (all except IGG). RESEARCH STAGES tab working with 55 tickers. "IN DASHBOARD" stat fixed to count masterData matches (shows 8 — NVTK not in coverage data). memoData nesting bug fixed (DCC/GET/PRY/DIE were inside EKTA, now top-level). Bake script insertion logic fixed. GH Pages NOT yet pushed — sandbox network returned 502 on GitHub. Local verification PASSED via localhost:8765 (Python HTTP server). Zero console errors. Richard gave 11 improvement requests for the RESEARCH STAGES tab just before handoff (listed below in full).

=== RICHARD'S 11 RESEARCH STAGES IMPROVEMENT REQUESTS (verbatim, 27-Apr-26) ===
1. Freeze column headers when scrolling down (sticky headers so they remain visible)
2. Explain what DASH column means (in-dashboard flag — ✓ if ticker has a masterData.stocks entry)
3. Explain what AUDIT column means (SOP compliance check — PASS/FAIL with issues tooltip)
4. Make ticker column wider
5. Duplicate company cleanup — e.g. AZA and AZA-SE are the same company, fix these
6. Group columns per STAGE with coloured box styling (matching how RATINGS view groups TIMELINESS, CASE STRENGTH etc. — uses .group-label CSS class with 1px solid #3b73c9 border, 3px radius, #f7f5ef bg)
7. Make each cell a hyperlink to the Notion note
8. Add date (MMM-YY format) in small text per cell showing when note was created
9. Add industry/sector column next to ticker (matching RATINGS view style)
10. Load all project files (done — patcher, scanner, state, decisions, visual guidelines all read)
11. Load all visual guidelines/principle files (done — memo-view-formatting-principles.md, memo-header-design-system-v5.md, memo-signposting-principles.md all read)

=== ACTIVE OPEN QUESTIONS (these block work — answer-or-park before authoring) ===
Q1. Duplicate tickers (AZA/AZA-SE etc.) — merge into primary ticker, keep richer + drop other, or flag for manual review? Proposed default: merge into primary ticker (higher word count wins per query).
Q2. Notion links per cell — coverage JSON doesn't store page_ids. Should we scan Files/ metadata to extract them, or skip for now? Proposed default: update build-coverage-data.py to extract page_ids from metadata.json files, rebuild coverage-data.json.
Q3. Sector source for all 55 tickers — pull from masterData (only 9 have it), add to coverage scanner, or hardcode a lookup table? Proposed default: add to coverage scanner with a lookup table embedded in build-coverage-data.py.
Q4. Cell dates — use file modified date, metadata.json created date, or skip for now? Proposed default: metadata.json created/date field (more accurate than mtime).

=== TODAY'S WORK, IN PRIORITY ORDER ===
A. Answer the 4 open questions above, then implement all 11 RESEARCH STAGES improvements in patch-coverage-tab.py (may also need build-coverage-data.py updates for Notion links, dates, sectors, duplicates).
B. Re-run the coverage scanner and patcher to regenerate the dashboard HTML.
C. Push to GitHub Pages (the 9-stock + memoData fix also still needs pushing).
D. GitHub push + verification + handoff.

=== STANDING OPERATING RULES (always-on for this session) ===
- Pre-write snapshot before any non-trivial mutation.
- Edit tool is BANNED on the dashboard HTML (~5MB); use bash+Python only.
- Zero ES6 constraint: var only, function(){} only, string concat only (iPad-safe).
- Patcher must remain idempotent (re-run produces +0 bytes).
- All marker-wrapped (COVERAGE_CSS_V1, COVERAGE_JS_V1, etc.).
- Coverage tab warm cream theme must match RATINGS view.
- Pillar group styling reference: .group-label class in RATINGS view (1px solid #3b73c9 border, 3px radius, #f7f5ef bg, 10px font, 600 weight, uppercase, #6b6b6b text).

=== BACK-BRIEF REQUIRED BEFORE ANY AUTHORING ===
After completing the orientation reads, back-brief me in ONE paragraph covering:
(a) what was shipped last session,
(b) what is still owed to me,
(c) the three questions at the top of your stack for me to answer.

Do NOT start authoring or mutating files until I have answered the open questions above.

=== RECOVERY-MODE FALLBACK (use only if memory files are unreachable) ===
If COWORK fails to mount or any of the orientation files are missing/corrupt:
- Active project: Ratings Dashboard
- Active stage: RESEARCH STAGES tab improvements (11 enhancement requests)
- Last shipped artefact: dashboard HTML 5,201,006 bytes, 9 stocks in masterData, 8 in memoData, RESEARCH STAGES with 55 tickers
- Last commit hash: d44c665 (STALE — has truncated file from Edit tool bug; local file is correct, needs re-push)
- Live dashboard URL: https://vfhqi.github.io/dashboards/ (currently serving stale/truncated version)
- Re-mount COWORK at C:\Users\richb\Documents\COWORK and report status before doing anything else.
```

## Session Summary

This session (continuation from context compaction) focused on three things: (1) fixing the dashboard so all 9 stocks appear in RATINGS with working MEMOs, (2) verifying locally via Chrome, and (3) receiving Richard's 11 improvement requests for the RESEARCH STAGES tab. The 9-stock dashboard is fully working locally but not yet pushed to GitHub Pages (sandbox network blocked). Richard gave a comprehensive list of RESEARCH STAGES improvements which will be the next session's work.

## Key Decisions

- memoData nesting bug discovered and fixed — the bake script's `}};` search found the wrong closing brace (inside EKTA) instead of the memoData object's closing brace. Fix applied to both the live HTML and the bake script itself.
- "IN DASHBOARD" stat now checks masterData.stocks instead of coverage data's in_dashboard field. Shows 8 (not 9 because NVTK isn't in the 55-ticker coverage scan — data gap, not code bug).
- Edit tool is confirmed BANNED on the ~5MB dashboard HTML. It truncated the file previously (commit d44c665 pushed truncated version to GH Pages). Always use Python scripts.
- Local verification approach established: run `python -m http.server 8765` in the databases/ folder, then Chrome extension can interact with `localhost:8765`.

## Corrections Logged

1. **bake-batch-v3-memos.py insertion point bug** — the script searched for `}};` in a 200-char window before MEMO_DATA_END and found one inside the last ticker's block (EKTA) rather than at the memoData object level. Fixed to use proper brace-matching from `memoData = {` opening brace. This is a code bug Watson created, not a Richard correction, but logged because it caused DCC/GET/PRY/DIE memos to nest inside EKTA.
2. **Watson tried to give Richard git clone instructions** — Richard correctly pointed out he doesn't use git from the command line and Watson should know the established GitHub push workflow (push script + PAT). Watson should always use the established tooling, not assume command-line git knowledge.

## Memory Files Updated

- `memory/projects/ratings-dashboard/state.md` — TL;DR updated to 9-STOCK DASHBOARD, fixes documented, audit results preserved
- `memory/projects/ratings-dashboard/audit-27apr26.md` — already existed from earlier in session, no changes needed
- `databases/scripts/bake-batch-v3-memos.py` — insertion point logic fixed (proper brace-matching)
- `databases/ic-ratings-dashboard-v2.html` — 6 stocks added to masterData, IN DASHBOARD stat fixed, memoData nesting fixed (5,201,006 bytes)

## Open Threads

- **GH Pages push pending** — local file is correct (5,201,006 bytes, 9 stocks, verified), but GH Pages still serves truncated version from commit d44c665. Needs push when network cooperates.
- **11 RESEARCH STAGES improvements** — Richard's full request list captured verbatim in restart prompt above. Four open questions need answering before work begins (duplicates handling, Notion links, sector source, date source).
- **NVTK missing from coverage data** — NVTK is in masterData.stocks but not in the 55-ticker coverage scan. Low priority.
- **Python HTTP server** — Richard may still have `python -m http.server 8765` running in a terminal. Can Ctrl+C to stop.

## Quality Audit

- **What was delivered:** 9-stock dashboard with all MEMOs working, IN DASHBOARD stat fixed, bake script fixed, local verification passed
- **Governing SOP:** Dashboard build SOPs (patch-coverage-tab.py idempotent patcher pattern, bake-batch-v3-memos.py for memo injection)
- **First-attempt pass rate:** No — memoData nesting bug required discovery + fix cycle
- **Rework details:** (1) Edit tool truncated dashboard → restored from backup + Python script. (2) Bake script nested DCC/GET/PRY/DIE inside EKTA → diagnosed via JS inspection in Chrome → Python dedup + restructure script. (3) Bake script insertion logic fixed for future runs.
- **Correction logged?** Yes — bake script bug + git instruction correction

## Meta-Role Observations

**SA integration check (silent — SA was primary role):**
- Structural changes made: bake script insertion logic rewritten, IN DASHBOARD stat JS logic changed, 6 masterData.stocks entries added
- All changes persisted to COWORK: state.md updated, bake script saved, dashboard HTML saved
- CLAUDE.md pipeline table: no changes needed (no stocks moved stages)
- No new skills created; existing session-handoff SKILL used for this handoff

**HPC observation (background):**
- Session involved significant troubleshooting (Edit tool truncation, memoData nesting, Chrome extension limitations, sandbox network failures). Persistence was good — each problem was identified and solved methodically. The localhost HTTP server workaround was a good adaptation.
