# Session Handoff — 27 Apr 2026 21:15
## SA - Ratings Dashboard | SA (primary) | EXECUTION

## Optimal Restart Prompt — copy-paste into next session

```text
Good morning. Watson, you are resuming SA - Ratings Dashboard from 27-Apr-26 21:15 BST.

Role: SA (primary). APM as secondary/background.
Mode: EXECUTION.
Agenda mode rationale: V5 RESEARCH STAGES tab is built and verified, SOP button fixed — remaining work is V6 visual polish (MM99-style group boxing, fit-to-screen), push, and data population. CARLB 29-Apr earnings is time-critical.

=== ORIENTATION (read in this order before responding) ===
1. COWORK/.claude/CLAUDE.md (always — operating context)
2. COWORK/memory/session-handoffs/latest.md (this handoff pointer)
3. COWORK/memory/projects/ratings-dashboard/state.md (live project state)
4. COWORK/memory/projects/ratings-dashboard/decisions.md (locked decisions — never re-ask)
5. COWORK/memory/projects/ratings-dashboard/checkpoint-27apr-1940.md (V5 verification results)
6. COWORK/memory/corrections.md (calibration points — new entry 27-Apr re SOP button)
7. .auto-memory/MEMORY.md (pointers to feedback rules)

=== WHERE WE LEFT OFF (verbatim — do not summarise without reading source files) ===
V5 RESEARCH STAGES tab is patched + verified (jsdom ALL PASS). Dashboard at 5,247,458 bytes (597-line patcher). THIS SESSION FIXED THE SOP BUTTON BUG: the ACTION column SOP copy-to-clipboard button was generating a generic IG prompt for all stocks regardless of their actual stage/next action. Fixed by replacing the hardcoded covSOPs dictionary with logic that reads next_action.detail from coverageData — each ticker now gets its correct, specific next-action prompt (e.g. AIXA gets "IG complete. Progress to Triaging by running RESEARCHER queries: Q4, Q5, Q6, Q7"; AENA gets "Triaging research queries complete. Run APM Analysis & Judgement SOP Phase 1-4..."). Patcher re-run: -940 bytes, scripts balanced, no var PB, ends </html>.

PRIOR to the SOP fix, the session (in compacted context) had begun the V6 visual overhaul. Richard sent 2 Chrome screenshots showing visual issues: (a) no coloured boxing around column groups, (b) ind/sec overlapping stage column, (c) ticker column broken, (d) horizontal scrolling. Watson extracted the MM99 group boxing pattern from master-dashboard/scripts/build_dashboard.py and designed the V6 CSS rewrite, but the SOP bug fix took priority. The V6 work is DESIGNED but NOT IMPLEMENTED.

V6 design notes (ready to implement):
- MM99 pattern: group header th cells get background:rgba(stageColor,0.08); first column in group gets border-left:2px solid rgba(stageColor,0.25); last column gets border-right:2px solid rgba(stageColor,0.25); header first/last get border-top too
- Stage colours for groups: IG rgba(107,107,107), Tri rgba(79,131,207), ESA rgba(121,85,191), DD rgba(176,107,30), Any rgba(120,144,156), APM rgba(201,195,176), Ratings rgba(59,115,201)
- Table fix: change width:max-content to width:100% with table-layout:fixed
- Ticker column: fix min-width/max-width + verify sticky left:0 works
- Ind/Sec column: fix left offset (currently hardcoded left:92px, needs to match actual ticker width)
- Research cells: compress to fit — try 36-42px instead of 42-58px
- New CSS classes needed: .cov-grp-ig-first, .cov-grp-ig-last, .cov-grp-tri-first, .cov-grp-tri-last, etc.
- JS functions covStageRow(), covQueryRow(), covDataRow() need updating to emit group border classes

Richard has NOT visually verified the tab. GitHub Pages NOT pushed (dashboard on GH Pages is stale, pre-V5).

=== ACTIVE OPEN QUESTIONS (these block work — answer-or-park before authoring) ===
Q1. Ticker suffix consistency: 4 tickers have country suffixes (ART-ES, CARLB-DK, FEVR-GB, PFSE-DE), remaining 47 do not. Proposed default: strip suffixes to match majority. Why: dashboard uses bare tickers everywhere else.
Q2. Priority column: Richard said "replace with 6 ratings" (Q6 from V5 brief). V5 keeps BOTH Priority AND ratings columns (43 cols). Proposed default: keep both — Priority shows next-action urgency, ratings show quality assessment. They serve different purposes. Why: removing Priority loses the action-urgency signal.
Q3. p4/p5/p6 ratings population: 8 dashboard stocks have p1/p2/p3 from masterData but p4 (Building Blocks), p5 (SS Earnings Momentum), p6 (Valuation) are empty. Proposed default: populate from RATINGS tab data if available, or leave blank until FCS is run. Why: these pillars may not have been assessed yet.
Q4. V6 visual overhaul scope: implement full MM99-style boxing + fit-to-screen, or do a lighter-touch CSS fix first? Proposed default: full V6 — it addresses all Richard's visual feedback in one pass.

=== TODAY'S WORK, IN PRIORITY ORDER ===
A. **CARLB 29-Apr earnings** — APM memo must ship before 29-Apr market open. This is time-critical and may take priority over dashboard work.
B. V6 visual overhaul — rewrite patcher CSS for: MM99-style bordered group boxes, table-layout:fixed to fit viewport, fix ticker+ind/sec positioning, uniform cell widths. All design work is done (see above).
C. Richard visually verifies RESEARCH STAGES tab in Chrome after V6 patch.
D. Push to GitHub Pages — try push-dashboard.sh or Chrome web UI upload.
E. Populate p4/p5/p6 ratings if data exists in RATINGS tab.

=== STANDING OPERATING RULES (always-on for this session) ===
- Edit tool is BANNED on files >~800KB; use bash+Python (feedback_edit_tool_truncation_bug).
- Pre-write snapshot before any non-trivial mutation.
- Save state checkpoint every 15 minutes during active work.
- Dashboard corruption pattern: always validate structure (no var PB, ends </html>, scripts balanced) before and after editing.
- Zero ES6 constraint: var only, function(){} only, string concat only. iPad-safe.
- Patcher is idempotent — safe to re-run.
- SOP button now reads next_action.detail — if coverage-data.json changes, re-run patcher to pick up new actions.

=== BACK-BRIEF REQUIRED BEFORE ANY AUTHORING ===
After completing the orientation reads, back-brief me in ONE paragraph covering:
(a) what was shipped last session,
(b) what is still owed to me,
(c) the three questions at the top of your stack for me to answer.

Do NOT start authoring or mutating files until I have answered the open questions above.

=== RECOVERY-MODE FALLBACK (use only if memory files are unreachable) ===
If COWORK fails to mount or any of the orientation files are missing/corrupt:
- Active project: SA - Ratings Dashboard
- Active stage: V5 RESEARCH STAGES tab complete + SOP bug fixed, V6 visual overhaul designed but not yet implemented
- Last shipped artefact: ic-ratings-dashboard-v2.html at 5,247,458 bytes (SOP fix patched 27-Apr-26 ~21:15 BST)
- Last commit hash: stale — needs push
- Live dashboard URL: https://vfhqi.github.io/dashboards/ic-ratings-dashboard-v2.html (NOT current — pre-V5)
- Re-mount COWORK at C:\Users\richb\Documents\COWORK and report status before doing anything else.
```

## Session Summary

This session had two phases. **Phase 1 (in compacted context):** Continued V6 visual overhaul work. Richard sent 2 Chrome screenshots showing visual issues in the RESEARCH STAGES tab (no column group boxing, ind/sec overlap, ticker column broken, horizontal scrolling). Watson extracted the MM99 group boxing pattern from master-dashboard/scripts/build_dashboard.py and designed the full V6 CSS rewrite. V5 patcher was backed up as `patch-coverage-tab-v5-backup.py`. The V6 rewrite was about to begin when Richard identified the SOP button bug. **Phase 2 (this context):** Fixed the SOP button bug. The ACTION column's copy-to-clipboard button was generating incorrect prompts — using deepest_stage to pick a generic template instead of reading the stock's actual next action from coverage data. Fixed, re-patched, verified, and wrote handoff.

## Work Completed

### 1. V6 Visual Overhaul — DESIGN ONLY (Phase 1, compacted context)
- **Backed up V5 patcher** as `databases/scripts/patch-coverage-tab-v5-backup.py`
- **Extracted MM99 group boxing pattern** from `master-dashboard/scripts/build_dashboard.py`:
  - Group header `<th>` cells: `background:rgba(R,G,B,0.08)` for tinted background
  - First column in group: `.grp-XX-first{border-left:2px solid rgba(color,0.25)}`
  - Last column in group: `.grp-XX-last{border-right:2px solid rgba(color,0.25)}`
  - Header first/last: `th.grp-XX-first,th.grp-XX-last{border-top:2px solid rgba(color,0.25)}`
- **Extracted master dashboard CSS variables** for colour matching: `--bg:#f7f5ef; --card:#fbfaf5; --border:#e8e3d4; --text:#333; --green:#2e7d32; --red:#c62828; --amber:#8d6e00; --blue:#1565c0; --purple:#7955bf`
- **Designed V6 CSS changes** (see restart prompt above for full spec)
- **NOT YET IMPLEMENTED** — interrupted by SOP bug fix

### 2. SOP Button Bug Fix (Phase 2, this context)
- **Bug:** `covDataRow()` line 525 derived `sopKey` from `stg` (deepest_stage), then looked up a generic template in `covSOPs`. For AIXA at IG stage needing Triaging, it copied "Run IG process for AIXA: Business Description (BD) + Change Forces (CF)..." — wrong. The `covSOPs` dictionary was a hardcoded 5-entry lookup (IG/Triaging/ESA/DD/FCS) that was a less-informed duplicate of the `compute_next_action()` function in `build-coverage-data.py`.
- **Fix:** Removed the `covSOPs` dictionary entirely. Replaced `covCopySOP(ticker, sopKey)` (2-arg) with `covCopySOP(ticker)` (1-arg) that reads `coverageData.tickers[ticker].next_action.detail` directly. Updated `covFlash()` to use ticker-only IDs (was `sop-TICKER-KEY`, now `sop-TICKER`). Updated `covDataRow()` SOP button to pass 1 arg.
- **Net change:** -940 bytes, patcher now 597 lines (was 606).

### 3. Verification
- Patcher re-run: 5,247,458 bytes (was 5,248,398), scripts balanced (1/1), ends `</html>`, no `var PB`
- Function signature check: `covCopySOP(ticker)` — 1 arg definition, 2 call sites, 0 legacy 2-arg calls, `covSOPs` dict absent
- Spot-checked 5 tickers against coverage-data.json:
  - **AIXA** (stage=IG): "IG complete. Progress to Triaging by running RESEARCHER queries: Q4, Q5, Q6, Q7. Ticker: AIXA" ✓
  - **AENA** (stage=Triaging): "Triaging research queries complete. Run APM Analysis & Judgement SOP Phase 1-4..." ✓
  - **DCC** (stage=Triaging, in dashboard): "All current. No action needed." ✓
  - **HTRO** (stage=Triaging): "Queries Q20,Q21 have research files but are not posted to Notion. Ticker: HTRO" ✓
  - **CARLB-DK** (stage=None): "All current. No action needed." ✓
- Dashboard structural validation: ends `</html>` ✓, no `var PB` ✓, scripts 1/1 ✓, "Minervini" 94 occurrences ✓

### 4. Files Saved This Session
| File | Action | Size/Lines |
|------|--------|------------|
| `databases/scripts/patch-coverage-tab.py` | Edited (SOP fix) | 597 lines |
| `databases/scripts/patch-coverage-tab-v5-backup.py` | Created (backup) | 606 lines |
| `databases/ic-ratings-dashboard-v2.html` | Re-patched | 5,247,458 bytes |
| `memory/projects/ratings-dashboard/state.md` | Updated TL;DR | — |
| `memory/projects/ratings-dashboard/handoffs/handoff-2026-04-27-2115.md` | This file | — |
| `memory/session-handoffs/latest.md` | Updated pointer | — |
| `memory/corrections.md` | Appended SOP correction | — |

## Key Decisions

- **No new decisions locked this session.** The fix follows from the existing design of `next_action.detail` in coverage-data.json. The V6 visual overhaul design follows from Richard's Chrome screenshots and the MM99 styling extraction.

## Corrections Logged

**One correction this session (appended to `memory/corrections.md`):**

### 2026-04-27 — [Structural / SOP Button — Use Data-Driven Next Action, Not Stage-Based Template]
- **Richard identified:** SOP button generates wrong prompt (IG for all stocks regardless of actual next action needed)
- **Root cause:** `covDataRow()` derived `sopKey` from `deepest_stage`, looked up hardcoded `covSOPs` dict — a parallel, less-informed system that duplicated `compute_next_action()` in `build-coverage-data.py`
- **Fix:** Removed `covSOPs` dict; `covCopySOP(ticker)` reads `next_action.detail` from coverage data directly
- **General lesson:** When the data layer already computes the right answer, the UI should read from data, not maintain a parallel lookup table

## Memory Files Updated

| File | What changed |
|------|-------------|
| `databases/scripts/patch-coverage-tab.py` | Removed `covSOPs` dict (5 entries), rewrote `covCopySOP` from 2-arg to 1-arg reading `next_action.detail`, updated `covFlash` to ticker-only IDs, updated `covDataRow` SOP button onclick |
| `databases/ic-ratings-dashboard-v2.html` | Re-patched with fixed SOP logic (5,247,458 bytes, -940 from V5) |
| `memory/projects/ratings-dashboard/state.md` | Updated TL;DR and added 27-Apr-26 21:15 SOP fix section |
| `memory/session-handoffs/latest.md` | Updated to point to this handoff |
| `memory/corrections.md` | Appended SOP button correction entry |

## Open Threads

### Unresolved — TIME-CRITICAL
1. **CARLB 29-Apr earnings.** APM memo must ship before 29-Apr market open (Tuesday). This was flagged as #1 priority in the prior EOD handoff and has not been addressed.

### Unresolved — Blocking (Dashboard)
2. **V6 visual overhaul NOT yet implemented.** Richard's Chrome screenshots showed 4 issues: (a) no coloured boxing around column groups — needs MM99-style bordered groups with tinted header backgrounds + border-left/right on first/last columns, (b) Industry/Sector column overlaps Stage column — `left:92px` sticky offset doesn't account for actual ticker width, (c) ticker column appears broken/cut off, (d) page has horizontal scrollbar — `width:max-content` on table forces wider than viewport. All design work is complete (MM99 pattern extracted, CSS classes designed, colour mappings ready). Implementation requires rewriting the CSS block (lines 58-178) and updating 3 JS functions (covStageRow, covQueryRow, covDataRow) to emit group border classes.
3. **Richard has NOT visually verified** the RESEARCH STAGES tab in Chrome. The SOP fix is functional (correct prompts), but the visual layout issues from his screenshots remain.
4. **GitHub Pages NOT pushed.** Dashboard on GH Pages is stale (pre-V5). Sandbox network blocked on prior attempts. Options: push-dashboard.sh, Chrome web UI upload, or Netlify deploy.

### Unresolved — Non-Blocking
5. **Ticker suffix inconsistency.** 4 of 51 tickers have country suffixes (ART-ES, CARLB-DK, FEVR-GB, PFSE-DE), rest are bare. Cosmetic.
6. **p4/p5/p6 ratings empty** for all 8 dashboard stocks. Only p1 (Technical Momentum), p2 (Market Paradigm Fit), p3 (Fundamental Change) populated. Building Blocks, SS Earnings Momentum, Valuation may not have been assessed yet.
7. **Priority column status.** Richard's V5 brief said "Replace with 6 ratings" — V5 keeps both Priority AND ratings (43 cols). Needs confirmation.
8. **3 company names potentially wrong:** BYLOT (shows Betsson AB — could be Byggfakta), YSN (shows YouScan — unverified), EVOK (corrected to Evolution AB from EvoK Betting in prior session).
9. **coverage-test-v5.html** in databases/ — test artefact from V5 verification, can be deleted.

## What Was Checked vs What Needs Checking

### Checked This Session
| Check | Result |
|-------|--------|
| `covCopySOP` function signature (1 arg) | PASS — `function covCopySOP(ticker)` |
| All call sites use 1 arg | PASS — 2 calls, 0 legacy 2-arg |
| `covSOPs` dictionary removed | PASS — not in dashboard |
| Dashboard ends `</html>` | PASS |
| No `var PB` corruption | PASS |
| Scripts balanced (1/1) | PASS |
| 5-ticker spot-check of next_action.detail | PASS — AIXA/AENA/DCC/HTRO/CARLB-DK all correct |
| Zero ES6 in patcher output | NOT CHECKED this session (was checked at V5 — no JS changes in fix affect ES6 compliance) |

### Needs Checking Next Session
| Check | Why |
|-------|-----|
| Visual layout in Chrome (V6 after CSS rewrite) | Richard's #1 visual complaint — column groups, overlap, scrolling |
| GitHub Pages deployment | Dashboard on GH Pages is pre-V5 |
| SOP button click in Chrome | Functional test — click SOP on AIXA, verify clipboard content |
| p4/p5/p6 data availability in RATINGS tab | Need to check if masterData has these ratings |
| Company name accuracy for BYLOT/YSN/EVOK | 3 potentially wrong mappings |

## Coverage Data Summary (for reference)

| Metric | Value |
|--------|-------|
| Tickers | 51 |
| In Dashboard | 8 (DCC, DIE, EKTA, ENAV, GET, HTRO, IGG, PRY) |
| Reports produced | 212 |
| High priority actions | 19 |
| Next action breakdown | researcher: 20, apm_memo: 19, monitoring: 7, notion_post: 5 |
| Priority breakdown | low: 20, high: 19, none: 7, medium: 5 |
| Notion URLs | 143 |
| Rating badges | 306 (24 non-blank) |

## Quality Audit

- **What was delivered:** Patched `patch-coverage-tab.py` (597 lines) + re-patched `ic-ratings-dashboard-v2.html` (5,247,458 bytes) with correct data-driven SOP button logic
- **Governing SOP:** "Do It Right" quality reform — TEST IT BEFORE YOU PRESENT IT
- **First-attempt pass rate:** Yes — patcher edit clean, re-run clean, all verification checks passed first time
- **Rework details:** None. Single-pass fix.
- **Correction logged?** Yes — SOP button stage-based template replaced with data-driven next_action.detail

## Meta-Role Observations

**SA role — structural integration checklist:**
- `patch-coverage-tab.py` — updated with SOP button fix ✓
- `patch-coverage-tab-v5-backup.py` — backup preserved ✓
- `ic-ratings-dashboard-v2.html` — re-patched ✓
- `state.md` — updated with SOP fix section ✓
- `decisions.md` — no new decisions to add ✓
- `corrections.md` — SOP button correction appended ✓
- `latest.md` — pointer updated ✓
- Auto-memory — no new feedback patterns to save (correction is project-specific, not a general Watson behaviour pattern) ✓

**Observation for next session:** The `compute_next_action()` function in `build-coverage-data.py` (lines 323-401) is the single source of truth for what each ticker needs next. It has a clear priority cascade: (1) ESA research done but no ESA memo → APM A&J SOP, (2) Triaging research done but no Triaging memo → APM A&J SOP, (3) memo exists but not in dashboard → bake, (4) unposted Notion queries → post, (5) IG done but no Triaging → run Triaging queries, (6) Triaging done with memo but no ESA → run ESA queries, (7) all good → monitoring. Any future changes to next-action logic should go through this function, not the UI layer.

**Chrome visual audit gap persists.** The Chrome MCP extension's inability to open local file:/// URLs means Watson cannot self-test visual layout. For V6, the test plan should be: (1) Watson writes + runs the V6 patcher, (2) Watson runs jsdom verification of structural correctness, (3) Richard opens the file in Chrome and reports visual issues, (4) Watson fixes. This is a two-party loop until the file:/// restriction is resolved.
